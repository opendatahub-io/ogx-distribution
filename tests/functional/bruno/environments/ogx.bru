vars {
  base_url: http://localhost:8321
  model:
  inference_provider:
  files_provider:
  vector_io_provider:
}
// Defaults are placeholders — override via CLI (--env-var) or copy values from .env.example
